# Runtime configuration lives in application.properties. Keep this profile only for
# environment-specific overrides when an explicit profile is used.
app.config.validation.strict=${APP_CONFIG_VALIDATION_STRICT:true}
